import React, { Fragment } from "react";
import icons from "./icons";
import { sequanceArray } from "./Grid";
export default function Controls({
  stalled,
  number,
  playMove,
  best,
  moveList,
  moves,
  go,
  deleteMove,
  showLevels,
  playLevel,
  resetClick
}) {
  const mainControls = <div className="controls">CONTROLS</div>;

  const body = mainControls;

  return body;
}
